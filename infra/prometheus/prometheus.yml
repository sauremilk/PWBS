# Prometheus configuration for PWBS monitoring (TASK-166).
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: pwbs-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
        labels:
          service: pwbs-api
