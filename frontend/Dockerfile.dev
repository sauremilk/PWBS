# PWBS Frontend  Development Dockerfile
# Hot-reload via next dev, source mounted as volume

FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package.json ./
RUN npm install

# Source is mounted as volume in docker-compose
# COPY . . is intentionally omitted for dev

EXPOSE 3000

CMD ["npm", "run", "dev"]
